#include <stdlib.h>
#include<stdio.h>

int main(int argc, const char* argv[])
{
    
	char *professor[32];
    int *student_ids, *grades;

    printf("What is your name?");
    scanf("%s", professor);
    printf();

    calloc(, );
}